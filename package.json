{
  "name": "app-statique",
  "version": "1.0.0",
  "private": true,
  "description": "Application Node : une seule page statique",
  "scripts": {
    "start": "node server.js",
    "audit:path": "node scripts/audit-path-safety.mjs",
    "test": "node --test tests/"
  },
  "engines": {
    "node": ">=18"
  }
}
